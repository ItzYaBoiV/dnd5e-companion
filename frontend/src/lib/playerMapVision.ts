/** Fallback sight ring / fog when `sightRadiusCells` is missing on a token. */
export const PLAYER_SIGHT_RING_CELLS = 10;
/** Fallback for expandFog when character data unavailable (cells). */
export const DEFAULT_PLAYER_VISION_FOG_CELLS = 6;
