#!/usr/bin/env node
/**
 * Dungeon Forge implementation lives in
 * frontend/src/components/dungeon-forge/DungeonForgeImpl.jsx.
 * This script remains so deploy.sh and `npm run sync-forge` keep a stable hook.
 */
console.log(
  "[build-dungeon-forge] No copy step — edit frontend/src/components/dungeon-forge/DungeonForgeImpl.jsx directly."
);
